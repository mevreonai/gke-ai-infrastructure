apiVersion: v1
kind: Service
metadata:
  name: deepseek-tp8-service
  namespace: default
  labels:
    app: deepseek-tp8-vllm
spec:
  type: ClusterIP
  selector:
    app: deepseek-tp8-vllm
  ports:
  - name: http
    protocol: TCP
    port: 8000
    targetPort: 8000
